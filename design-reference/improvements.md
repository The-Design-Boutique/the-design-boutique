# Design Improvement Notes (require Angelo's approval before applying)

Per the mandate to maintain the current design exactly, nothing here is acted on without a yes. This is a running list of things observed on the live site that look broken, inconsistent, or worth improving, captured during reference work so they are not silently changed and not silently ignored.

_None logged yet. Populated during design-reference capture and build._
