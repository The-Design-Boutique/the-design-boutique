

# Core Web Vitals (SOW 2.2)
# Free key from Google Cloud: enable "Chrome UX Report API", then create an API key.
CRUX_API_KEY=
# Optional: the origin to measure. Defaults to the live site.
CWV_ORIGIN=https://thedesignboutique.com
# Shared secret so the dashboard's "Refresh now" button can call the collection route.
CRON_SECRET=
